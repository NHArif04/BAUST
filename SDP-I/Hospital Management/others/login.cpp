#include<iostream>
#include <fstream> 
#include<string>

using namespace std;

int count=0;

int main(){
    ifstream inFile("user.txt");
    inFile>>count
    if(inFile.is_open()){

    }

    return 0;
}